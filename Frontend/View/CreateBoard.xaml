<Window x:Class="Frontend.View.CreateBoard"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Frontend.View"
        mc:Ignorable="d"
        Title="CreateBoard" Height="400" Width="600" MinHeight="200" MinWidth="300">
    <Viewbox Stretch="Uniform">
        <Grid>
            <StackPanel Margin="10">
                <Label Content="CREATE BOARD" FontSize="36" HorizontalAlignment="Center"/>
                <Grid Margin="0,10,0,0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*" />
                    </Grid.ColumnDefinitions>
                    <Grid.RowDefinitions>
                        <RowDefinition Height="Auto"/>
                        <RowDefinition Height="Auto"/>
                    </Grid.RowDefinitions>
                    <Label Content="Board Name:" Grid.Row="1" Grid.Column="0" FontSize="20" VerticalAlignment="Center"/>
                    <TextBox Grid.Row="1" Grid.Column="1" Margin="5" 
                             Text="{Binding Path=BoardName, Mode=OneWayToSource}" />
                </Grid>
                <Button Content="CREATE" Width="100" Margin="0,10,0,0" 
                        HorizontalAlignment="Center" Click="CreateBoard_Click"/>
                <Label Margin="0,10,0,0" Content="{Binding Path=ErrorMessage, Mode=OneWay}" 
                       Foreground="Red" HorizontalAlignment="Center"/>
                <Button Content="Back" Click="Back_Click" FontSize="20" 
                        Margin="0,20,0,0" Width="100" HorizontalAlignment="Center"/>
            </StackPanel>
        </Grid>
    </Viewbox>
</Window>
